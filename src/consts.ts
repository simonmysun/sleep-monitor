export const SECOND_IN_MS = 1000;
export const MINUTE_IN_MS = SECOND_IN_MS * 60;
export const HOUR_IN_MS = MINUTE_IN_MS * 60;
export const DAY_IN_MS = HOUR_IN_MS * 24;
export const WEEK_IN_MS = DAY_IN_MS * 7;

export const TAU_MIN = 18;
export const TAU_MAX = 30;

export const PHASE_SLEEP_CENTER = Math.PI; // 最容易睡的相位

export const HISTORY_DAYS = 45;
